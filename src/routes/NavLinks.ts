interface Routes {
  [key: string]: string
}

const routs: Routes = {
  home: '/',
  projects: '/projects',
  members: '/parties',
  organizations: '/publicOrgs',
  grants: '/grants',
  vacancies: '/vacancies',
  messages: '/messages',
  userProfile: '/profile',
  createProject: '/createProject',
  registration: '/registration'
}

export default routs
